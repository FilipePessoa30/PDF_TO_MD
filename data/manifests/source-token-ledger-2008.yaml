# Source-token ledger (PROMPT Fase 3Q, Section 8).
#
# Machine-readable record of individual tokens whose textual fidelity
# required low-level adjudication (rawdict/texttrace/font-fingerprint
# evidence) before being restored to canonical text - distinct from
# layout-overrides.yaml (which documents *where* a line is protected from
# region-membership exclusion) and blocker-ledger-2008.yaml (which
# documents the *causal narrative* of each defect). This ledger documents
# *token identity itself*: charcode, glyph ID, font fingerprint, and the
# exact backend evidence that proves a character is what it appears to be,
# independent of whether the correction mechanism (override, general fix,
# glyph mapping, or visual fallback) is documented elsewhere.
#
# Never used to justify a correction by itself - see layout-overrides.yaml
# for the actual `protect_from_region_membership` entries this evidence
# supports. A token entry exists purely as an auditable identity record.
schema_version: 1
tokens:
  - token_id: q24-item-marker-I
    question_id: enade-2008-computing-q24
    source_page: 12
    expected_role: item_marker
    raw_unicode: "I"
    charcode: 73
    glyph_id: 35
    font_name: PKFJJH+TT2EC3o00
    font_fingerprint: sha256:343d7586022dff8987613f327f457dbfc4c8cad430d340b0fe4d6fda949d0980
    bbox: [46.67958068847656, 261.61260986328125, 49.438499450683594, 271.5726013183594]
    origin: [46.67958068847656, 269.279052734375]
    baseline: 269.279052734375
    source_backend: pymupdf-rawdict+texttrace
    first_divergent_stage: region_membership
    extraction_status: raw_text_verified
    canonical_representation: "I"
    visual_fallback: null
    evidence: >-
      page.get_text("rawdict"): span text="I", font=TT2EC3o00, size=9.96,
      flags=4 (superscript=0, italic=0, serifed=0, monospaced=0). char
      c="I", origin=(46.68, 269.28), bbox matches span bbox exactly (single
      glyph). page.get_texttrace(): same span, chars=((73, 35, origin,
      bbox),) - charcode=73 (ASCII/WinAnsiEncoding "I"), glyph_id=35.
      page.get_fonts(full=True): font xref 18, ('cff','Type1',
      'PKFJJH+TT2EC3o00','F5','WinAnsiEncoding',0) - a standard,
      non-custom encoding (no ToUnicode CMap override needed; WinAnsi's
      own codepoint 73 is defined as LATIN CAPITAL LETTER I in the PDF
      spec itself). Font file extracted via doc.extract_font(18): CFF/
      Type1, 12043 bytes, sha256
      343d7586022dff8987613f327f457dbfc4c8cad430d340b0fe4d6fda949d0980.
      Identical font+charcode+glyph_id as sibling item markers "II" (2x)
      and "III" (3x) on the same page, the latter of which already
      survives, unmodified, to the published corpus - proving this exact
      font/encoding pair is already trusted, unambiguous body content in
      this same question. No OCR, no LLM, no semantic inference used.
    validation: reviewed
  - token_id: q24-item-marker-II
    question_id: enade-2008-computing-q24
    source_page: 12
    expected_role: item_marker
    raw_unicode: "II"
    charcode: [73, 73]
    glyph_id: [35, 35]
    font_name: PKFJJH+TT2EC3o00
    font_fingerprint: sha256:343d7586022dff8987613f327f457dbfc4c8cad430d340b0fe4d6fda949d0980
    bbox: [45.23910140991211, 348.61224365234375, 50.754947662353516, 358.5722351074219]
    origin: [45.23910140991211, 356.2786865234375]
    baseline: 356.2786865234375
    source_backend: pymupdf-rawdict+texttrace
    first_divergent_stage: region_membership
    extraction_status: raw_text_verified
    canonical_representation: "II"
    visual_fallback: null
    evidence: >-
      page.get_text("rawdict"): span text="II", font=TT2EC3o00, size=9.96,
      flags=4, two chars both c="I" (origins (45.24,356.28) and
      (48.00,356.28)). page.get_texttrace(): same span, chars=((73, 35,
      origin1, bbox1), (73, 35, origin2, bbox2)) - both charcode=73,
      glyph_id=35, identical to marker "I" above and to sibling marker
      "III" (see q24-item-marker-I's own evidence for the shared font
      fingerprint). Same font xref (18), same encoding (WinAnsiEncoding),
      no custom mapping needed.
    validation: reviewed
  - token_id: q45-item-iii-function-formula
    question_id: enade-2008-computing-q45
    source_page: 19
    expected_role: inline_formula
    raw_unicode: null
    charcode: null
    glyph_id: null
    font_name: null
    font_fingerprint: null
    bbox: [406.679, 481.919, 442.919, 493.439]
    origin: null
    baseline: null
    source_backend: pymupdf-get_drawings
    first_divergent_stage: not_applicable_never_was_text
    extraction_status: visual_only_verified
    canonical_representation: null
    visual_fallback: >-
      enade-2008-computing-q45/figure-02.png (RESOLVED Phase 3S - a NEW,
      dedicated, tightly-cropped asset, type equation, rendered directly
      from this exact bbox and inserted inline at the correct point in
      item III's own reading order, between "o grafico de" and "e as
      retas x = 0 e x = 2." - superseding the Phase 3Q note this field
      used to carry, which pointed at the pre-existing figure-01.png -
      that asset happens to also show this formula, but is scoped to the
      intro's own two solid-of-revolution diagrams, sits far earlier in
      the statement, and includes far more surrounding text than this one
      formula alone: a faithful visual record, but not a correctly-placed
      one. figure-01.png itself is completely unmodified by this
      resolution.
    evidence: >-
      page.get_drawings() reports 9 vector path elements (fill+stroke
      glyph-shaped outlines for "f", "(", "x", ")", "=", the radical
      "sqrt" checkmark+overbar, and the second "x") whose own union bbox
      is (406.679,481.919)-(442.919,493.439), filling exactly the gap
      between "o grafico de" (ends x1=404.97, y0=483.28) and "e as retas
      x = 0 e x = 2." (starts x0=443.97, same y0=483.28).
      page.get_text("rawdict")/get_texttrace() report NO character/span/
      glyph anywhere in this x-range on this row - confirming this is
      100% vector-drawn artwork, never a font character with a charcode/
      glyph_id/font to recover. Direct visual inspection of the newly
      rendered figure-02.png (Phase 3S) confirms the drawing renders as
      the italic formula "f(x) = sqrt(x)" (a function definition,
      consistent with item III's own printed sentence "...o grafico de
      f(x) = sqrt(x) e as retas x = 0 e x = 2."). No text recovery is
      possible or attempted - inserting a literal "f(x) = sqrt(x)" string
      into canonical text (structured_content/canonical_representation)
      would be an unverifiable inference from the drawing's own rendered
      shape, not a demonstrated character identity, and is explicitly not
      done (PROMPT Phase 3Q Section 5/17, reaffirmed Phase 3S Section 5).
      Accessibility limitation (PROMPT Fase 3S Section 15): this formula
      is preserved visually only - it has no extracted text representation
      and no validated LaTeX, and is therefore not full-text-searchable as
      a formula; this limitation is recorded here, not hidden. This exact
      convention (an inline formula rendered as vector art, never
      converted to text) already exists twice more in this same,
      already-published Q45 statement, deliberately NOT touched by this
      phase (out of its own declared scope - only this one, previously
      blocked, formula was addressed): "(f $ 0)" for "(f(x) >= 0)" in the
      intro, and "como resultado da integral ." (the integral formula
      itself, between "integral" and the trailing period) - both remain
      exactly as before, confirming this is this booklet's own consistent,
      pre-existing convention for inline math, not a new or inconsistent
      standard being applied here.
    validation: reviewed
  - token_id: d40-tree-selection-operator-sigma
    question_id: enade-2008-computing-d40
    source_page: 17
    expected_role: diagram_operator_symbol
    raw_unicode: "F"
    charcode: 70
    glyph_id: 2
    font_name: PKHEMP+TT2F14o00
    font_fingerprint: sha256:99573bc2e5d21945e581d503837be3ab37b4ed735018da36448f7e49efbdfbfa
    bbox: [95.75910186767578, 574.5380859375, 103.20065307617188, 587.9138793945312]
    origin: [95.75910186767578, 585.2387084960938]
    baseline: 585.2387084960938
    source_backend: pymupdf-rawdict+texttrace+get_fonts+extract_font+get_pixmap
    first_divergent_stage: region_membership
    extraction_status: glyph_verified
    canonical_representation: null
    visual_fallback: enade-2008-computing-d40/figure-01.png (already rendered
      and published - the region's own asset-render clip in assets.py's
      `_render_bbox` extends past this glyph's own x0, independently of the
      narrower logical VisualRegion.bbox `_line_in_region` uses for text
      exclusion, so the true glyph is already fully visible there)
    evidence: >-
      page.get_text("rawdict"): span text="F", font=TT2F14o00, size=13.31,
      flags=4, single char c="F", origin=(95.759, 585.239), bbox
      (95.759,574.538)-(103.201,587.914). page.get_texttrace(): same span,
      chars=((70, 2, origin, bbox),) - charcode=70 (WinAnsiEncoding "F",
      PyMuPDF's own fallback decode since this font carries no ToUnicode
      CMap), glyph_id=2. page.get_fonts(full=True): font xref 160,
      ('cff','Type1','PKHEMP+TT2F14o00','F66','WinAnsiEncoding',0) - a
      367-byte embedded CFF/Type1 SUBSET font (doc.extract_font(160) sha256
      99573bc2e5d21945e581d503837be3ab37b4ed735018da36448f7e49efbdfbfa),
      used on this page for exactly two glyphs: charcode 70/glyph_id 2
      (this token) and charcode 66/glyph_id 1 ("B", bbox
      123.239,536.978-131.515,550.354, immediately above this token in the
      same query-tree diagram, sitting over "nome,endereco" the way this
      token sits over "idade < 40 OR renda < 30000"). Rendering this exact
      page region via page.get_pixmap() at the same 3x zoom
      enade.extraction.assets.RENDER_ZOOM already uses (i.e. inspecting the
      already-published data/questions/2008/all-computing/
      enade-2008-computing-d40/figure-01.png) shows neither glyph is a
      Latin letter at all: the rendered image reads "pi_nome,endereco" /
      "sigma_idade < 40 OR renda < 30000" / "Cliente" - the pi/sigma
      relational-algebra projection/selection operators, exactly matching
      the question's own already-published sentence "na qual B e F
      representam as operacoes de projecao e de selecao, respectivamente"
      (this token IS the "F" that sentence itself already names as a
      selection-operator symbol, not a coincidence). WinAnsi's own
      codepoint 70 is nominally LATIN CAPITAL LETTER F, but a subsetted,
      custom-glyph-program font is free to paint any shape at any glyph
      index regardless of its nominal encoding table entry - confirmed
      here not by trusting the declared encoding (which would be exactly
      the kind of raw-charcode assumption q24-item-marker-I/II's own
      entries rely on safely, only because those markers' fonts are NOT
      custom symbol subsets) but by directly rendering the glyph program
      itself and visually comparing it against the source PDF's own
      painted page. No OCR, no LLM, no semantic/linguistic inference used -
      the identity comes from the font's own embedded glyph program,
      rendered by the same PDF engine (PyMuPDF/MuPDF) already trusted to
      produce every other published asset in this corpus.
    validation: reviewed
