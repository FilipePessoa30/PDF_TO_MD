# Declarative, hash-and-bbox-locked exceptions to general layout heuristics
# (PROMPT Phase 2B section 6, Level 3). See
# src/enade/extraction/layout_overrides.py for the loader and matching
# rules, and docs/decisions.md (Phase 2B ADR) for why this specific case
# needed Level 3 rather than a general fix.
overrides:
  - pdf_sha256: eb3b497f0d0967b1b6d063784c3012e719d1758ba182e4f6d6e6e47151934548
    page: 14
    bbox: [100.0, 115.0, 215.0, 138.0]
    rule: exclude_from_orphan_marker_merge
    question_id: enade-2011-computing-q22
    reason: >-
      Truth-table header row ("A B C D S", each a bare single-character
      cell at y0~119.8) is real table structure, not a split circled-letter
      alternative marker. Two general (Level 1) geometric heuristics were
      tried and rejected after full 2021 byte-diff regression testing found
      real false positives: (1) a minimum-glyph-width check changed 3
      already-certified 2021 files (Q23, Q34, D4); (2) a "3+ short cells
      share this y0" row heuristic still changed 2021 Q23, because that
      question's own binary-tree figure has 3 independent node labels
      ("R", "L", "A") that coincidentally share a y0 by diagram layout, not
      because they are a table row. Neither discriminator generalizes
      safely, so this exact bbox on this exact PDF is excluded explicitly
      instead.
    evidence: docs/phase-2b-report.md, Section G
    status: reviewed
  # NOTE (Phase 2C): the Q13 suppress_visual_region override that lived here
  # (bbox [0,250,300,650]) is superseded by the ownership model
  # (ownership.py + figures.py's owner-aware detect_visual_regions): the
  # tiny raster images that used to seed a region spanning Q11-Q12-Q13 are
  # now assigned to their true owner (Questao 12) before any merge/growth,
  # and growth is hard-clipped to Questao 12's own claimed territory - it
  # can no longer reach Q11's alternatives or Q13's pseudocode regardless
  # of label proximity. Removing the override (rather than leaving it
  # alongside the new mechanism) let Questao 12 recover its own formula
  # content, previously suppressed along with the spurious region. See
  # docs/decisions.md, Phase 2C ADR (ownership model).
  - pdf_sha256: eb3b497f0d0967b1b6d063784c3012e719d1758ba182e4f6d6e6e47151934548
    page: 14
    bbox: [540.0, 125.0, 560.0, 148.0]
    rule: protect_from_label_absorption
    question_id: enade-2011-computing-q23
    reason: >-
      The word "e" trailing the alphabet-notation inline image
      ("...alfabeto [Sigma={a,b,c}] e") sits at x0=547.3 - far from either
      of page 14's two detected column margins (30, 295) because it is a
      mid-sentence continuation after an inline symbol image, not the
      start of a physical line. The existing two-column body-text
      protection (_is_two_column_body_text) only recognizes a line by its
      own starting x0 matching a known margin, so this single-word
      fragment was eligible for absorption into the automaton diagram's
      region, silently dropping "e" from the statement text.
    evidence: docs/phase-2b-report.md, Section I
    status: reviewed
  - pdf_sha256: eb3b497f0d0967b1b6d063784c3012e719d1758ba182e4f6d6e6e47151934548
    page: 14
    bbox: [330.0, 150.0, 380.0, 170.0]
    rule: protect_from_label_absorption
    question_id: enade-2011-computing-q23
    reason: >-
      Same defect class as the "e" fragment above: ", em que" (x0=333.8)
      trails the "Sigma*" inline symbol image mid-sentence
      ("...subconjunto de [Sigma*], em que [lambda] representa..."), so it
      does not start at either detected column margin and was eligible for
      label absorption.
    evidence: docs/phase-2b-report.md, Section I
    status: reviewed
  - pdf_sha256: eb3b497f0d0967b1b6d063784c3012e719d1758ba182e4f6d6e6e47151934548
    page: 14
    bbox: [388.0, 150.0, 510.0, 170.0]
    rule: protect_from_label_absorption
    question_id: enade-2011-computing-q23
    reason: >-
      Same defect class again: "representa o string vazio." (x0=392.8)
      trails the "lambda" inline symbol image on the same line as the
      ", em que" fragment above, so it too does not start at either
      detected column margin.
    evidence: docs/phase-2b-report.md, Section I
    status: reviewed
  # NOTE (Phase 2E): the three exclude_from_region_candidates overrides that
  # lived here (Sigma={a,b,c}/Sigma*/lambda, bboxes ~488-550x129-146,
  # 313-330x154-166, 377-391x154-166) are superseded by the small-formula-
  # image mechanism (Phase 2C, figures.py's own SMALL_IMAGE_MAX_WIDTH/
  # _HEIGHT + MIN_FORMULA_WIDTH/_HEIGHT pool) reaching general availability
  # after this override was first written (Phase 2B, before that pool
  # existed): each symbol image is 12-57pt wide x ~12pt tall, comfortably
  # within SMALL_IMAGE_MAX_WIDTH(250)/_HEIGHT(40) and MIN_FORMULA_WIDTH(4)/
  # _HEIGHT(4) - it no longer needs the large-candidate MIN_REGION_HEIGHT
  # (20pt) the original reason cited, and small-formula candidates never
  # participate in the large-candidate chain-merge that used to re-anchor
  # the automaton region's own top edge. Removing the exclusion let each
  # symbol form its own small, correctly-scoped region instead of being
  # discarded before ever reaching classification. See docs/decisions.md,
  # Phase 2E ADR 41.
  # NOTE: the Q1/Q2 force_single_column_page override that previously lived
  # here was removed after detect_column_margins gained a general Y-range-
  # overlap requirement (layout.py) that fixes this case - and a second,
  # independently-discovered one on Discursiva 4/page 19 - without a
  # per-document override. See docs/decisions.md, Phase 2B ADR 24/25.
  - pdf_sha256: eb3b497f0d0967b1b6d063784c3012e719d1758ba182e4f6d6e6e47151934548
    page: 5
    bbox: [40.0, 368.0, 285.0, 378.0]
    rule: protect_from_label_absorption
    question_id: enade-2011-computing-q06
    reason: >-
      Questao 6's infographic ("o Xis da questao") is a vector chart whose
      own true extent ends around y=352 (its "Fontes: Manager Assessoria
      em Recursos Humanos e IBGE" credit line). Its caption, "Disponivel
      em: <http://ead.uepb.edu.br/noticias,82>. Acesso em: 24 ago. 2011."
      (x0=40.0, indented off the page's own left margin of 28.5 because
      captions are conventionally indented under a figure), sits 16pt
      below that and was close enough to be absorbed as a figure label,
      which then bridged the gap to Questao 6's own statement text below
      it (see the next two overrides) - the first domino in a longer
      chain-absorption than any single-cause override could stop cleanly.
    evidence: docs/phase-2b-report.md, Section K
    status: reviewed
  - pdf_sha256: eb3b497f0d0967b1b6d063784c3012e719d1758ba182e4f6d6e6e47151934548
    page: 5
    bbox: [28.0, 387.0, 288.0, 401.0]
    rule: protect_from_label_absorption
    question_id: enade-2011-computing-q06
    reason: >-
      "A expressao 'o Xis da questao' usada no titulo do" - the first line
      of Questao 6's own statement tail (the sentence that leads into its
      alternatives) - sits at x0=28.5, exactly the page's left column
      margin, so it should already have been protected by
      _is_two_column_body_text; it was not, because that check runs before
      label absorption even begins to chain from the caption above (see
      the previous override) which reaches it first via vertical
      proximity. Once absorbed, this line (and its own wrapped second
      line, protected separately below) silently vanished from Questao
      6's rendered statement, truncating it mid-sentence.
    evidence: docs/phase-2b-report.md, Section K
    status: reviewed
  - pdf_sha256: eb3b497f0d0967b1b6d063784c3012e719d1758ba182e4f6d6e6e47151934548
    page: 5
    bbox: [28.0, 399.0, 129.0, 413.0]
    rule: protect_from_label_absorption
    question_id: enade-2011-computing-q06
    reason: >-
      "infografico diz respeito" - the wrapped second line of the same
      statement-tail sentence as the previous override, completing "A
      expressao 'o Xis da questao' usada no titulo do infografico diz
      respeito". Same defect class and same fix.
    evidence: docs/phase-2b-report.md, Section K
    status: reviewed
  - pdf_sha256: eb3b497f0d0967b1b6d063784c3012e719d1758ba182e4f6d6e6e47151934548
    page: 5
    bbox: [45.0, 428.0, 257.0, 443.0]
    rule: protect_from_label_absorption
    question_id: enade-2011-computing-q06
    reason: >-
      "garantir um emprego estavel com salario digno." is the wrapped
      second line of alternative A, indented to x0=45.5 (not the column
      margin of 28.5, so _is_two_column_body_text does not protect it, and
      not wide enough to trigger _is_paragraph_continuation's >300pt
      previous-line-width signal, calibrated for single-column pages).
      TEXT_ABSORPTION_PADDING (90pt) is wide enough to bridge the 62.6pt
      gap from the chart's own true bottom edge (y=366.3) directly to this
      line even with the two overrides above already in place, after
      which three more alternative-continuation lines below it
      (identically unprotected, for the same reason) chain-absorb in turn
      until MAX_ABSORPTION_GROWTH (110pt beyond the original bbox) caps
      growth at y=476.3 - deep into Questao 6's own alternative block, and
      close enough in X (9.3pt gap) to also false-attach across the
      column boundary into Questao 7 (which has no real figure at all;
      see the region-suppression note in the Phase 2B report, Section K -
      residual limitation not fully resolved by this override alone).
      Protecting this one bridge line stops the chain at its true origin.
    evidence: docs/phase-2b-report.md, Section K
    status: reviewed
  - pdf_sha256: eb3b497f0d0967b1b6d063784c3012e719d1758ba182e4f6d6e6e47151934548
    page: 5
    bbox: [45.0, 455.0, 264.0, 470.0]
    rule: protect_from_label_absorption
    question_id: enade-2011-computing-q06
    reason: >-
      "que aumenta o nivel de escolaridade dos individuos." - alternative
      B's own wrapped continuation line, same defect class as alternative
      A's continuation above. Its gap from the chart's true bottom edge
      (y=366.3) is 89.5pt - just under TEXT_ABSORPTION_PADDING (90pt) - so
      it independently bridges the same absorption chain even with
      alternative A's continuation separately protected, since each
      candidate is tested against the current bbox independently rather
      than in a fixed sequence. Both bridge lines must be protected
      together to keep the region at its true extent.
    evidence: docs/phase-2b-report.md, Section K
    status: reviewed
  # --- Phase 2D: Q10's 5 stacked two-line fractions -----------------
  # A general (Level 1) fix was tried and rejected here in Phase 2C
  # (spurious fractions on 2021 Q34's Dijkstra graph node-label listing -
  # see docs/decisions.md, ADR 30). These 5 entries are the Section 8
  # "perfil declarativo... preso ao SHA-256" alternative: each pairs one
  # alternative's own marker bbox with its own numerator's own bbox,
  # explicitly, with no search/distance heuristic at all - they can only
  # ever match these 5 exact lines, on this exact PDF. The existing
  # general orphan-marker search still finds each marker's own denominator
  # (unchanged); this override supplies only the numerator that search
  # does not look for.
  - pdf_sha256: eb3b497f0d0967b1b6d063784c3012e719d1758ba182e4f6d6e6e47151934548
    page: 9
    bbox: [298.0, 307.0, 323.0, 326.0]
    secondary_bbox: [330.0486145019531, 302.1814880371094, 341.1686096191406, 315.4914855957031]
    rule: force_fraction_merge
    question_id: enade-2011-computing-q10
    reason: >-
      Alternative A's own marker "A" (bbox ~300.7,309.8-321.0,323.1) sits
      between its own numerator "61" (above, y0=302.2) and denominator
      "73" (below, y0=317.5) - a stacked fraction, never a single line
      with a slash glyph in the source. The general merge already finds
      "73" as the marker's primary partner (closest in Y); this override
      supplies "61" explicitly so the rendered alternative reads "61/73"
      instead of just "73".
    evidence: docs/decisions.md, Phase 2D ADR 31 (force_fraction_merge)
    status: reviewed
  - pdf_sha256: eb3b497f0d0967b1b6d063784c3012e719d1758ba182e4f6d6e6e47151934548
    page: 9
    bbox: [298.0, 342.0, 323.0, 360.0]
    secondary_bbox: [330.0486145019531, 336.7420959472656, 341.1686096191406, 350.0520935058594]
    rule: force_fraction_merge
    question_id: enade-2011-computing-q10
    reason: Same defect class as alternative A above, for alternative B's own "61/155".
    evidence: docs/decisions.md, Phase 2D ADR 31 (force_fraction_merge)
    status: reviewed
  - pdf_sha256: eb3b497f0d0967b1b6d063784c3012e719d1758ba182e4f6d6e6e47151934548
    page: 9
    bbox: [298.0, 376.0, 323.0, 395.0]
    secondary_bbox: [330.0486145019531, 371.30267333984375, 341.1686096191406, 384.6126708984375]
    rule: force_fraction_merge
    question_id: enade-2011-computing-q10
    reason: Same defect class as alternative A above, for alternative C's own "67/155".
    evidence: docs/decisions.md, Phase 2D ADR 31 (force_fraction_merge)
    status: reviewed
  - pdf_sha256: eb3b497f0d0967b1b6d063784c3012e719d1758ba182e4f6d6e6e47151934548
    page: 9
    bbox: [298.0, 411.0, 323.0, 429.0]
    secondary_bbox: [330.0486145019531, 405.8631896972656, 341.1686096191406, 419.1731872558594]
    rule: force_fraction_merge
    question_id: enade-2011-computing-q10
    reason: Same defect class as alternative A above, for alternative D's own "22/112".
    evidence: docs/decisions.md, Phase 2D ADR 31 (force_fraction_merge)
    status: reviewed
  - pdf_sha256: eb3b497f0d0967b1b6d063784c3012e719d1758ba182e4f6d6e6e47151934548
    page: 9
    bbox: [298.0, 446.0, 323.0, 464.0]
    secondary_bbox: [330.0486145019531, 440.4237976074219, 341.1686096191406, 453.7337951660156]
    rule: force_fraction_merge
    question_id: enade-2011-computing-q10
    reason: Same defect class as alternative A above, for alternative E's own "67/112".
    evidence: docs/decisions.md, Phase 2D ADR 31 (force_fraction_merge)
    status: reviewed
  # --- Phase 2D: Q12's item IV cut off by MAX_ABSORPTION_GROWTH -----
  - pdf_sha256: eb3b497f0d0967b1b6d063784c3012e719d1758ba182e4f6d6e6e47151934548
    page: 10
    bbox: [44.0, 607.0, 289.0, 623.0]
    rule: protect_from_region_membership
    question_id: enade-2011-computing-q12
    reason: >-
      Item IV's own opening line ("IV. Nas cadeias geradas por essa
      gramatica, todos os") sits exactly where the region seeded by
      Q12's own formula images (see the ownership-model note above) hits
      MAX_ABSORPTION_GROWTH's 110pt cap - measured directly: the region's
      one "large" pre-expansion candidate (the BNF-grammar raster image)
      has its own y1=499.29; 499.29+110=609.29, exactly the final
      region's own y1. This is a hard growth ceiling, not a consequence
      of item IV itself being absorbed as a label (confirmed: adding a
      protect_from_label_absorption override for this same bbox did not
      change the region's final bbox at all, since items I-III alone
      already reach the cap without it) - so the fix has to stop
      _line_in_region's own overlap check from treating item IV's line as
      "inside" the region, not stop it from being absorbed. Item IV's own
      first line overlaps the growth-capped edge by under 1pt (608.4 vs
      609.29), wide enough with REGION_Y_PADDING's 2pt tolerance to be
      excluded from the statement text even though the rendered crop
      itself does not show it either (RENDER_PADDING's own 4pt does not
      reach far enough to make it legible). Protecting it from region
      membership directly keeps it as plain text, where it is fully
      legible; the image crop's own extent is unaffected (still complete
      for the intro sentence, grammar, and items I-III).
    evidence: docs/decisions.md, Phase 2D ADR 32 (protect_from_region_membership)
    status: reviewed
  # --- Phase 2D: Q45's own analysis items I-V absorbed as circuit labels
  - pdf_sha256: eb3b497f0d0967b1b6d063784c3012e719d1758ba182e4f6d6e6e47151934548
    page: 29
    bbox: [44.0, 308.0, 205.0, 324.0]
    rule: protect_from_label_absorption
    question_id: enade-2011-computing-q45
    reason: >-
      Q45's own op-amp circuit diagram (a real embedded raster image,
      natural extent y=110.8-263.0) grows via label absorption to
      y0=67.5-y1=373.0 - reaching MAX_ABSORPTION_GROWTH's 110pt cap below
      (263.0+110=373.0, matching exactly) by absorbing items I-V of Q45's
      own analysis list as if each were a diagram annotation (each is
      narrow, non-monospace, non-chrome, not at a two-column margin - the
      same geometric shape as a real figure label). This drags the
      region's own Y-range across the whole statement, excluding the
      intro sentence, the "Considerando..." sentence, and items II-V from
      the rendered statement even though most of them are individually
      too wide to have been absorption candidates themselves - once the
      region's bbox reaches their own Y position via the items that WERE
      absorbed, plain geometric overlap excludes them regardless.
      Protecting item I's own marker line here (its own statement text,
      "A corrente i1 e idealmente nula.", already happens to escape via
      an unrelated, coincidental match against the A-E alternative-marker
      pattern - protecting the marker line itself too for consistency and
      auditability, not reliance on that coincidence).
    evidence: docs/decisions.md, Phase 2D ADR 32 (protect_from_label_absorption growth-cap diagnostic)
    status: reviewed
  - pdf_sha256: eb3b497f0d0967b1b6d063784c3012e719d1758ba182e4f6d6e6e47151934548
    page: 29
    bbox: [44.0, 323.0, 205.0, 339.0]
    rule: protect_from_label_absorption
    question_id: enade-2011-computing-q45
    reason: Same defect class as item I above, for item II's own line.
    evidence: docs/decisions.md, Phase 2D ADR 32 (protect_from_label_absorption growth-cap diagnostic)
    status: reviewed
  - pdf_sha256: eb3b497f0d0967b1b6d063784c3012e719d1758ba182e4f6d6e6e47151934548
    page: 29
    bbox: [44.0, 338.0, 266.0, 354.0]
    rule: protect_from_label_absorption
    question_id: enade-2011-computing-q45
    reason: Same defect class as item I above, for item III's own line.
    evidence: docs/decisions.md, Phase 2D ADR 32 (protect_from_label_absorption growth-cap diagnostic)
    status: reviewed
  - pdf_sha256: eb3b497f0d0967b1b6d063784c3012e719d1758ba182e4f6d6e6e47151934548
    page: 29
    bbox: [44.0, 353.0, 446.0, 368.0]
    rule: protect_from_label_absorption
    question_id: enade-2011-computing-q45
    reason: Same defect class as item I above, for item IV's own line.
    evidence: docs/decisions.md, Phase 2D ADR 32 (protect_from_label_absorption growth-cap diagnostic)
    status: reviewed
  - pdf_sha256: eb3b497f0d0967b1b6d063784c3012e719d1758ba182e4f6d6e6e47151934548
    page: 29
    bbox: [44.0, 368.0, 418.0, 383.0]
    rule: protect_from_label_absorption
    question_id: enade-2011-computing-q45
    reason: Same defect class as item I above, for item V's own line.
    evidence: docs/decisions.md, Phase 2D ADR 32 (protect_from_label_absorption growth-cap diagnostic)
    status: reviewed
  # --- Phase 2D: Q44's own trailing paragraphs/equations absorbed as labels
  - pdf_sha256: eb3b497f0d0967b1b6d063784c3012e719d1758ba182e4f6d6e6e47151934548
    page: 28
    bbox: [27.0, 449.0, 320.0, 464.0]
    rule: protect_from_label_absorption
    question_id: enade-2011-computing-q44
    reason: >-
      Q44's own 8-bit adder circuit diagram (natural extent
      y=148.0-408.2) grows via label absorption to y1=518.2 - exactly
      408.2+MAX_ABSORPTION_GROWTH(110). "tempo, o multiplexador pode
      entregar o resultado mais rapidamente." is narrow enough
      (290.4pt < 300pt) to be an absorption candidate itself, and is the
      first domino: once absorbed, the chain reaches the two short
      sentences introducing each equation below it (see the next two
      overrides), and their own small formula images (already correctly
      classified as size-based "formula" candidates, but still pulled
      into a Y-overlap merge with the by-then-larger diagram region -
      see figures.py's _merge_overlapping_regions) plus the wide (>500pt)
      paragraphs above and below all end up geometrically inside the
      grown region's own Y-range regardless of their own individual
      absorbability.
    evidence: docs/decisions.md, Phase 2D ADR 32 (protect_from_label_absorption growth-cap diagnostic)
    status: reviewed
  - pdf_sha256: eb3b497f0d0967b1b6d063784c3012e719d1758ba182e4f6d6e6e47151934548
    page: 28
    bbox: [27.0, 466.0, 255.0, 482.0]
    rule: protect_from_label_absorption
    question_id: enade-2011-computing-q44
    reason: Same defect class as above, for "Considere as seguintes equacoes dos somadores:".
    evidence: docs/decisions.md, Phase 2D ADR 32 (protect_from_label_absorption growth-cap diagnostic)
    status: reviewed
  - pdf_sha256: eb3b497f0d0967b1b6d063784c3012e719d1758ba182e4f6d6e6e47151934548
    page: 28
    bbox: [230.0, 483.0, 254.0, 499.0]
    rule: protect_from_label_absorption
    question_id: enade-2011-computing-q44
    reason: >-
      Same defect class as above, for the single-word "e" connecting the
      two somador equation images ("S = A xor B xor VemUm" "e"
      "VaiUm = A.B + VemUm.A + VemUm.B").
    evidence: docs/decisions.md, Phase 2D ADR 32 (protect_from_label_absorption growth-cap diagnostic)
    status: reviewed
  - pdf_sha256: eb3b497f0d0967b1b6d063784c3012e719d1758ba182e4f6d6e6e47151934548
    page: 28
    bbox: [27.0, 500.0, 291.0, 517.0]
    rule: protect_from_label_absorption
    question_id: enade-2011-computing-q44
    reason: Same defect class as above, for "Considere, ainda, a equacao dos multiplexadores a seguir:".
    evidence: docs/decisions.md, Phase 2D ADR 32 (protect_from_label_absorption growth-cap diagnostic)
    status: reviewed
  # --- Phase 2D: Q48's own connecting sentence excluded by a growth-capped region
  - pdf_sha256: eb3b497f0d0967b1b6d063784c3012e719d1758ba182e4f6d6e6e47151934548
    page: 31
    bbox: [27.0, 349.0, 206.0, 366.0]
    rule: protect_from_region_membership
    question_id: enade-2011-computing-q48
    reason: >-
      Q48's own UML use-case diagram (natural extent y=137.2-334.5) hits
      MAX_ABSORPTION_GROWTH's own 110pt cap regardless of any single
      candidate (334.5+110=444.5, matching the final region's own y1
      exactly, confirmed by testing that a protect_from_label_absorption
      override for this same bbox did not change the region's final bbox
      at all - the same finding as Q12's own item IV, see that override's
      own reason). "Da analise do diagrama, conclui-se que" (the sentence
      leading into the alternatives) overlaps the growth-capped region
      geometrically even though it was never itself an absorption
      candidate that mattered. protect_from_region_membership addresses
      the exclusion check directly rather than absorption eligibility.
      The alternatives themselves (A-E) are unaffected (already protected
      by the existing alternative-marker/alternatives-section exemptions).
    evidence: docs/decisions.md, Phase 2D ADR 32 (protect_from_region_membership)
    status: reviewed
  # --- Phase 2E: Q38's own "N -> Nd" / "N -> ·Nd" productions swallowed by
  # a small-formula region's own exclusion padding, not rendered by it either
  - pdf_sha256: eb3b497f0d0967b1b6d063784c3012e719d1758ba182e4f6d6e6e47151934548
    page: 25
    bbox: [28.4, 296.4, 65.7, 313.9]
    rule: protect_from_region_membership
    question_id: enade-2011-computing-q38
    reason: >-
      Production 4 of Q38's own grammar ("N -> Nd") is real, correctly
      decoded text (TimesNewRomanPS-ItalicMT, chars 'N' 0x4e + 'd' 0x64,
      confirmed by direct rawdict inspection) sitting immediately below
      production 3 ("E -> Nx"). Production 3's own terminal "x" is a tiny
      embedded raster image (bbox ~59.6,292.7-69.9,300.4, ~10x8pt) that
      the existing small-formula-image mechanism (Phase 2C) correctly
      detects and crops as figure-05.png - but that region's own bbox
      (292.7-300.4, only 7.7pt tall) sits close enough to production 4's
      own top edge (y0=299.6) that REGION_Y_PADDING (2pt,
      assembler.py) is enough to mark the whole "N -> Nd" line as
      "inside" the region and exclude it from the statement text, even
      though the rendered crop (RENDER_PADDING only reaching y=304.4)
      never shows enough of it to be legible either - the line was
      neither readable as text nor as image. Confirmed by direct
      before/after comparison of the assembled statement.
      protect_from_region_membership exempts this specific line from
      that exclusion check directly - the same mechanism already used
      for Q12's own item IV and Q48's own connecting sentence (both
      Phase 2D, MAX_ABSORPTION_GROWTH cap cases) - here applied to a
      small-formula region's own tight bbox instead of a growth-capped
      large region, but the same "the exclusion check, not the region
      itself, needs to be fixed" diagnosis applies.
    evidence: docs/decisions.md, Phase 2E ADR 39 (small-formula region membership)
    status: reviewed
  - pdf_sha256: eb3b497f0d0967b1b6d063784c3012e719d1758ba182e4f6d6e6e47151934548
    page: 25
    bbox: [28.4, 400.3, 68.9, 418.8]
    rule: protect_from_region_membership
    question_id: enade-2011-computing-q38
    reason: >-
      Same defect class as the "N -> Nd" override above, for the e0
      state's own analogous production "N -> ·Nd" (y0=403.5), swallowed
      by the small-formula region carrying e0's own "E -> ·Nx" (bbox
      ~62.8,396.6-73.2,404.3; 404.3+REGION_Y_PADDING(2)=406.3 >= 403.5).
    evidence: docs/decisions.md, Phase 2E ADR 39 (small-formula region membership)
    status: reviewed
  - pdf_sha256: eb3b497f0d0967b1b6d063784c3012e719d1758ba182e4f6d6e6e47151934548
    page: 25
    bbox: [28.4, 469.8, 75.5, 488.3]
    rule: protect_from_region_membership
    question_id: enade-2011-computing-q38
    reason: >-
      Same defect class as the two overrides above, for the e1 state's
      own third production "M -> N·d" (y0=469.9), swallowed by the
      small-formula region carrying e1's own "M -> N·x" (bbox
      ~67.5,462.5-77.9,470.2; 470.2+REGION_Y_PADDING(2)=472.2 >= 469.9).
      Found via a systematic page-25 sweep (every Q38-owned small-formula
      region cross-checked against every Q38-owned line for this same Y-
      proximity signature) after the first two instances were found
      individually - not assumed to be the only ones.
    evidence: docs/decisions.md, Phase 2E ADR 39 (small-formula region membership)
    status: reviewed
  - pdf_sha256: eb3b497f0d0967b1b6d063784c3012e719d1758ba182e4f6d6e6e47151934548
    page: 25
    bbox: [28.4, 251.6, 131.4, 265.1]
    rule: protect_from_region_membership
    question_id: enade-2011-computing-q38
    reason: >-
      "d representa um digito." (y0=251.7), the closing clause of the
      sentence naming Q38's own grammar symbols, is real text
      (ArialMT) sitting immediately below the small-formula region
      carrying the "M, N, E" non-terminal symbols and the "x"/"d"
      terminal symbols (bbox ~89.5,234.1-247.0,252.3;
      252.3+REGION_Y_PADDING(2)=254.3 >= 251.7). Found in the same
      systematic sweep as the two "N -> Nd"-class overrides above -
      confirmed missing from both the plain statement text and
      figure-02.png's own crop (which ends at "...em que", one line
      short).
    evidence: docs/decisions.md, Phase 2E ADR 39 (small-formula region membership)
    status: reviewed
  - pdf_sha256: eb3b497f0d0967b1b6d063784c3012e719d1758ba182e4f6d6e6e47151934548
    page: 25
    bbox: [28.4, 161.1, 287.5, 174.6]
    rule: protect_from_region_membership
    question_id: enade-2011-computing-q38
    reason: >-
      "interpretada como doze em base dez ou como dezoito em" (y0=161.2),
      the third line of Q38's own opening paragraph, sits immediately
      below figure-01's own region (bbox ~28.0,140.9-279.6,161.9;
      161.9+REGION_Y_PADDING(2)=163.9 >= 161.2) - the same MAX_
      ABSORPTION_GROWTH-driven wide-crop pattern as figure-01 itself
      (which legitimately captures the two lines above this one), but
      the crop's own extent stops one line short. Confirmed missing from
      both the plain statement text and figure-01.png's own crop.
    evidence: docs/decisions.md, Phase 2E ADR 39 (small-formula region membership)
    status: reviewed
  # --- Phase 3K: D10's own newspaper-collage page (2008-b) - page-scoped
  # activation of reading_zones.zoned_reading_order. SUPERSEDED IN PHASE
  # 3L: the rule this entry names (force_zoned_reading_order_page) is no
  # longer consulted by any code - LayoutOverrideSet.forces_zoned_reading_order
  # was removed once assembler.py gained a structural eligibility detector
  # (reading_zones.assess_eligibility) that selects D10's own span
  # automatically, from the same observable zone evidence described below,
  # never from this page/hash record. Kept here, not deleted, as the
  # historical record of the page-scoped fix that first resolved D10
  # safely (PROMPT Phase 3L section 12: "não apague o histórico do
  # override; marque-o como superseded"). See docs/phase-3l-report.md for
  # the full account of why page/hash selection was replaced.
  - pdf_sha256: 5c8a08f7fdaccaa3b5661f40705e214bffdab634b845bce6288f8eeaa25db264
    page: 7
    bbox: [0.0, 0.0, 0.0, 0.0]
    rule: force_zoned_reading_order_page
    question_id: enade-2008-computing-d10
    reason: >-
      D10's own page ("Formacao Geral Discursiva 10") opens with a photo
      beside a two-column newspaper article, continues as full-width
      single-column prose for two more motivating fragments, then closes
      with a genuine two-column bulleted "Observacoes" box - three
      distinct column topologies on one page. detect_column_margins's own
      single, page-wide (left_margin, right_margin) decision (35.0, 250.0)
      applied unconditionally pushes every line at or past x0=245 to
      after every line before it, scrambling the article/prose/bullets
      sequence into unrelated fragments interleaved out of order (see
      docs/phase-3k-report.md for the full forensic trace). reading_zones
      resolves this by requiring genuine, concurrent two-sided evidence
      within each specific vertical window rather than one page-wide
      split - confirmed to reproduce D10's own correct reading order
      without disturbing any other 2008-b page or 2011/2021 (neither of
      which sets zoned_reading_order_gate at all). SUPERSEDED (Phase 3L):
      D10 is now selected by structural eligibility instead - see the
      note above.
    evidence: docs/phase-3k-report.md; docs/phase-3l-report.md
    status: superseded
  # --- Phase 3O: region-merge-content-loss cluster (2008-b) ---------------
  # Every entry below is `protect_from_region_membership`: the line is
  # genuinely inside a legitimate visual region's own final bbox (the
  # region/asset itself is correct and untouched), but the line's own
  # content is real canonical prose/code, not part of what the region's
  # own image is meant to represent - see docs/phase-3o-report.md, Section
  # 13/20 for the full evidentiary reasoning (LineRegionRelation's own new
  # `looks_like_body_prose` field already resolves the sibling cases where
  # a body-prose line was, mechanically, absorbed only via a font-size-gate
  # rounding-noise false positive: 2008-b's own real body prose renders at
  # a raw 9.96pt (never a clean 10.0), which still rounds to the same
  # "10.0" bucket `_dominant_body_font_size` reports as this booklet's own
  # body size, but compares as strictly *less than* an un-rounded 10.0
  # threshold in figures.py's own `caption_font_size_gate` (zero margin) -
  # silently admitting Q02's own "Essa afirmativa reitera..." sentence,
  # Q07's own "De acordo com o mesmo grafico..." clause, and Q54's own
  # opening clause as legitimate label-absorption candidates even though
  # none of them is caption-shaped content at all. A general, disposition-
  # layer fix for this exact rounding defect (`LineRegionRelation.
  # looks_like_body_prose`, gating `_text_consumption_decision` on font-
  # size evidence alone) was tried and rejected: full corpus regeneration
  # found two real regressions an isolated 7-question check did not surface
  # - 2008-b Q1's own "IV"/"V" picture labels (bare 1-2 char roman
  # numerals, coincidentally also 9.96pt) and Q73's own neighboring-
  # question paragraph (long, genuinely body-prose-shaped, but not Q73's
  # own statement) both would have newly leaked into canonical text. Font
  # size alone is not sufficient evidence to authorize consumption
  # corpus-wide (see `_text_consumption_decision`'s own docstring); these
  # three lines get the same individually-verified
  # `protect_from_region_membership` treatment as every other entry in
  # this group instead. The remaining entries below cover the residual
  # cases that were never a rounding-bug question in the first place: a
  # genuinely caption-sized citation absorbed exactly as designed
  # (Q02/Q05/Q07's own image citations - geometrically and mechanically
  # indistinguishable from 2008-b D10's own "Revista Veja..." photo
  # credit, which correctly stays hidden; the difference is evidentiary,
  # not geometric - each citation below is the *only* record of the
  # image's own source/context, never reproduced elsewhere in the
  # question, per direct visual audit), and a handful of real framing
  # sentences/code lines caught inside a single oversized region formed by
  # `_merge_by_vertical_proximity` legitimately joining several separate
  # diagram/table fragments (or, for D40, an unrelated far-column rascunho
  # ruled grid) under this booklet's own generous region_merge_x_tolerance
  # (150pt) - the region and its rendered crop are correct and left
  # untouched; only these specific lines' own exclusion is undone.
  - pdf_sha256: 5c8a08f7fdaccaa3b5661f40705e214bffdab634b845bce6288f8eeaa25db264
    page: 3
    bbox: [53.0, 123.0, 277.0, 136.0]
    rule: protect_from_region_membership
    question_id: enade-2008-computing-q02
    reason: >-
      "Essa afirmativa reitera a necessaria interacao das" (y0=124.5,
      9.96pt body prose, the first physical line of the statement's own
      transition sentence into the image) - absorbed as a label candidate
      purely by the font-size-gate rounding defect described above (its
      raw 9.96pt size compares as "smaller than" the page's own rounded
      10.0 body threshold), then excluded from canonical text by plain
      geometric containment in the resulting grown region.
    evidence: docs/phase-3o-report.md, Section M
    status: reviewed
  - pdf_sha256: 5c8a08f7fdaccaa3b5661f40705e214bffdab634b845bce6288f8eeaa25db264
    page: 3
    bbox: [39.0, 135.0, 290.0, 147.0]
    rule: protect_from_region_membership
    question_id: enade-2008-computing-q02
    reason: >-
      "diferentes especies, representadas na imagem a seguir." (y0=136.2),
      the second physical line of the same sentence as the override above -
      same defect class, same fix. Missed in this phase's own first pass
      (only the first line was found and protected, cutting the restored
      sentence short at "das" - caught by
      tests/test_extraction_pipeline_2008.py::test_q02_statement_and_citation_are_now_complete,
      which checks the full sentence rather than a partial substring).
    evidence: docs/phase-3o-report.md, Section M
    status: reviewed
  - pdf_sha256: 5c8a08f7fdaccaa3b5661f40705e214bffdab634b845bce6288f8eeaa25db264
    page: 3
    bbox: [133.0, 407.0, 276.0, 416.0]
    rule: protect_from_region_membership
    question_id: enade-2008-computing-q02
    reason: >-
      "Disponivel em http://curiosidades.spaceblog.com.br." (y0=408.8,
      6pt) is Q02's own image citation - the only record of the source,
      never reproduced elsewhere in the statement/alternatives. Absorbed
      as a genuine label-absorption candidate (correctly - it is real
      caption-shaped content, 6pt vs. the page's own 9.96pt body prose),
      then excluded from canonical text by the same geometric-containment
      check that also, correctly, keeps 2008-b D10's own "Revista Veja..."
      photo credit hidden - the two are mechanically identical
      (`state=contained`, `matches_absorbed_label=True`,
      `raw_intersects=False` in both cases) and no general geometric
      signal separates them (confirmed: same absorption pass, same
      region-relation fields, see docs/phase-3o-report.md Section 7/13).
      D10's own credit line is genuinely decorative relative to its own
      question (an illustrative photo for an essay prompt, never
      referenced by the essay's own instructions); Q02's citation is the
      sole attribution for the image the statement explicitly discusses
      ("...representadas na imagem a seguir" / "Depreende-se dessa
      imagem a..."), so its loss is a real information gap, not a
      redundant credit line duplicating the image itself.
    evidence: docs/phase-3o-report.md, Section M
    status: reviewed
  - pdf_sha256: 5c8a08f7fdaccaa3b5661f40705e214bffdab634b845bce6288f8eeaa25db264
    page: 3
    bbox: [207.0, 414.0, 276.0, 423.0]
    rule: protect_from_region_membership
    question_id: enade-2008-computing-q02
    reason: >-
      "Acesso em 10 out. 2008." (y0=415.9), the second physical line of
      the same citation as the override above - same defect class, same
      fix.
    evidence: docs/phase-3o-report.md, Section M
    status: reviewed
  - pdf_sha256: 5c8a08f7fdaccaa3b5661f40705e214bffdab634b845bce6288f8eeaa25db264
    page: 3
    bbox: [390.0, 628.0, 559.0, 638.0]
    rule: protect_from_region_membership
    question_id: enade-2008-computing-q05
    reason: >-
      "STRICKLAND, Carol; BOSWELL, John. Arte Comentada: da" (y0=629.3,
      6pt) is Q05's own image citation, first physical line - same defect
      class as Q02's own citation above (genuine label-absorption
      candidate, then wrongly excluded from canonical text by geometric
      containment alone). The asset's own `caption`/`alt_text` fields
      remain null (PROMPT Phase 3O Section 15 does not yet have a general
      mechanism to populate them from an absorbed label automatically);
      restoring the citation to the canonical statement text is the
      `canonical_only`-equivalent safe direction until that mechanism
      exists.
    evidence: docs/phase-3o-report.md, Section N
    status: reviewed
  - pdf_sha256: 5c8a08f7fdaccaa3b5661f40705e214bffdab634b845bce6288f8eeaa25db264
    page: 3
    bbox: [390.0, 636.0, 559.0, 645.0]
    rule: protect_from_region_membership
    question_id: enade-2008-computing-q05
    reason: >-
      "pre-historia ao pos-moderno. Rio de Janeiro: Ediouro [s.d.]." (y0=
      637.7), the second physical line of the same citation as the
      override above - same defect class, same fix.
    evidence: docs/phase-3o-report.md, Section N
    status: reviewed
  - pdf_sha256: 5c8a08f7fdaccaa3b5661f40705e214bffdab634b845bce6288f8eeaa25db264
    page: 4
    bbox: [271.0, 644.0, 561.0, 656.0]
    rule: protect_from_region_membership
    question_id: enade-2008-computing-q07
    reason: >-
      "De acordo com o mesmo grafico, o percentual da renda" (y0=645.0,
      9.96pt body prose) - the statement's own transition clause into the
      question actually being asked, absorbed as a label candidate purely
      by the font-size-gate rounding defect described above (same
      mechanism as Q02's own sentence override), then excluded from
      canonical text by plain geometric containment in the resulting
      grown region.
    evidence: docs/phase-3o-report.md, Section O
    status: reviewed
  # NOTE (Phase 3O): Q07's own chart citation, "Disponivel em
  # http://www.ipea.gov.br" (page 4, 6pt), is NOT region-excluded at all in
  # the real pipeline, even without any override - confirmed both by
  # `git show HEAD:.../enade-2008-computing-q07.md` (the citation already
  # reads "E. 80%. Disponivel em http://www.ipea.gov.br" in the corpus as
  # published before this phase) and by direct trace: `_in_alternatives_section`
  # (assembler.py) independently exempts it once its own y0 (753.2, below
  # even alternative E's own marker at 745.2) places it past the
  # alternatives section's own start - no override needed or possible to
  # change that. The real defect is `assemble_question`'s own alternative-
  # boundary slicing (the per-letter `alt_bounds` index range from one
  # marker to the next, `text_only_lines`-position-based, with no X-
  # proximity check of its own - unlike `_in_alternatives_section`'s
  # separate exemption test), which sweeps this citation into alternative
  # E's own tail purely by list position, regardless of it living in a
  # different page column entirely - real alternative-text contamination,
  # exactly the failure mode this blocker's own Phase 3H note already named
  # ("alternative E's own citation contamination... unrelated to Cluster
  # D"). A general fix (making the per-letter slice itself X-proximity-
  # aware, matching `_in_alternatives_section`'s own existing logic) would
  # touch core alternative-boundary detection shared by every objective
  # question in every booklet - out of scope for a first, disposition-only
  # pass (Section 23) and not attempted here. This is a pre-existing defect
  # this phase neither caused nor fixed; left exactly as published. Tracked
  # as q07-alternative-e-citation-contamination-risk and in
  # docs/phase-3o-report.md, Section O.
  # --- Phase 3O: Q24's own oversized raw-merge (decoder table + 3 item
  # circuits merged into one region by y-proximity) swallows its own
  # framing paragraph -------------------------------------------------
  - pdf_sha256: 5c8a08f7fdaccaa3b5661f40705e214bffdab634b845bce6288f8eeaa25db264
    page: 12
    bbox: [36.0, 195.0, 298.0, 207.0]
    rule: protect_from_region_membership
    question_id: enade-2008-computing-q24
    reason: >-
      "Considere o bloco decodificador ilustrado acima, o qual" (y0=196.5,
      9.96pt body prose). Q24's decoder truth table and its 3 item
      circuits (I/II/III) are 4 separate diagrams that
      `_merge_by_vertical_proximity` correctly groups into one connected
      region (y=105.0-522.5) - each item's own circuit sits close enough
      in Y to the next to be a genuine single connected cluster, and the
      resulting single crop (figure-01.png) is itself correct and
      unchanged by this override. This framing paragraph's own 4 lines
      (this one and the 3 below) sit geometrically inside that region's
      Y-range purely because the diagrams above and below them are far
      apart on the page - `raw_intersects` is True (genuine overlap with
      the merged raw extent, not just growth padding), so the fix is at
      the exclusion-check layer (`protect_from_region_membership`, the
      same mechanism as 2011 Q12's own item IV / Q48's own connecting
      sentence), never the merge itself.
    evidence: docs/phase-3o-report.md, Section P
    status: reviewed
  - pdf_sha256: 5c8a08f7fdaccaa3b5661f40705e214bffdab634b845bce6288f8eeaa25db264
    page: 12
    bbox: [36.0, 207.0, 298.0, 219.0]
    rule: protect_from_region_membership
    question_id: enade-2008-computing-q24
    reason: >-
      "opera segundo a tabela apresentada. Em cada item a seguir," (y0=208.3) - second line of the same framing paragraph as above.
    evidence: docs/phase-3o-report.md, Section P
    status: reviewed
  - pdf_sha256: 5c8a08f7fdaccaa3b5661f40705e214bffdab634b845bce6288f8eeaa25db264
    page: 12
    bbox: [36.0, 219.0, 298.0, 231.0]
    rule: protect_from_region_membership
    question_id: enade-2008-computing-q24
    reason: >-
      "julgue se a funcao logica mostrada corresponde ao circuito" (y0=220.1) - third line of the same framing paragraph as above.
    evidence: docs/phase-3o-report.md, Section P
    status: reviewed
  - pdf_sha256: 5c8a08f7fdaccaa3b5661f40705e214bffdab634b845bce6288f8eeaa25db264
    page: 12
    bbox: [36.0, 231.0, 139.0, 243.0]
    rule: protect_from_region_membership
    question_id: enade-2008-computing-q24
    reason: >-
      "logico a ela associado." (y0=232.0) - fourth and last line of the same framing paragraph as above.
    evidence: docs/phase-3o-report.md, Section P
    status: reviewed
  # --- Phase 3Q: Q24's own item markers "I"/"II" (page 12) - genuinely
  # present, unambiguous text (charcode 73 = "I" in WinAnsiEncoding, font
  # PKFJJH+TT2EC3o00, sha256 343d7586022dff8987613f327f457dbfc4c8cad430d340b0fe4d6fda949d0980
  # - the identical font/charcode/glyph as sibling marker "III", which
  # already survives to the published corpus), excluded purely by a
  # razor-thin geometric margin: item III's own marker glyph run is wider
  # (three "I" glyphs vs one/two) and starts 1.28-2.76pt further left than
  # I/II's own narrower runs, placing III's own left edge (x0=43.9) just
  # outside the merged region's own grown bbox (x0=45.2) - enough to drop
  # its own `intersection_over_line_area` under the 0.98 "contained"
  # threshold (state=`boundary_crossing` instead), while I/II's own
  # slightly-narrower/more-indented glyph runs (x0=45.2/46.7) stay fully
  # enclosed (state=`contained`) and get excluded by
  # `_text_consumption_decision`'s own first, unconditional branch. Not
  # `raw_intersects` (the region's own *raw*, pre-growth extent never
  # reaches this far down - Q24's own 4-diagram merge, unchanged since
  # Phase 3O, legitimately spans y=105-522.5) - a pure growth-vs-glyph-
  # width coincidence, confirmed by direct instrumentation of
  # `_line_in_region`/`compute_line_region_relation` against the real,
  # unchanged region object. See data/manifests/source-token-ledger-2008.yaml
  # for the full low-level identity evidence (rawdict/texttrace/font
  # fingerprint) - both markers are 100% textually unambiguous; this is a
  # region-membership defect, never a glyph/OCR recovery problem.
  - pdf_sha256: 5c8a08f7fdaccaa3b5661f40705e214bffdab634b845bce6288f8eeaa25db264
    page: 12
    bbox: [44.0, 260.0, 51.0, 273.0]
    rule: protect_from_region_membership
    question_id: enade-2008-computing-q24
    reason: >-
      Item marker "I" (bbox 46.68,261.61-49.44,271.57, charcode 73="I",
      font PKFJJH+TT2EC3o00) - genuinely, unambiguously present text,
      excluded only because its own bbox is fully enclosed
      (`state=contained`) by the merged region's own grown bbox
      (45.2,105.0-290.0,522.5), a coincidence of this specific glyph run's
      own narrower width relative to sibling marker "III" (see the group
      comment above for the full geometric account).
    evidence: docs/phase-3q-report.md, Section H; data/manifests/source-token-ledger-2008.yaml
    status: reviewed
  - pdf_sha256: 5c8a08f7fdaccaa3b5661f40705e214bffdab634b845bce6288f8eeaa25db264
    page: 12
    bbox: [44.0, 347.0, 52.0, 360.0]
    rule: protect_from_region_membership
    question_id: enade-2008-computing-q24
    reason: >-
      Item marker "II" (bbox 45.24,348.61-50.75,358.57, two chars each
      charcode 73="I", same font as above) - same defect class as marker
      "I" above.
    evidence: docs/phase-3q-report.md, Section H; data/manifests/source-token-ledger-2008.yaml
    status: reviewed
  # --- Phase 3O: Q45's own oversized raw-merge (solid-of-revolution
  # figure + item diagrams) swallows its own framing clauses and items
  # I/II's own body text ------------------------------------------------
  - pdf_sha256: 5c8a08f7fdaccaa3b5661f40705e214bffdab634b845bce6288f8eeaa25db264
    page: 19
    bbox: [303.0, 181.0, 426.0, 193.0]
    rule: protect_from_region_membership
    question_id: enade-2008-computing-q45
    reason: >-
      "como resultado da integral" (y0=182.3, 9.96pt body prose) - the
      intro paragraph's own closing clause, immediately followed by a
      small inline formula image and this sentence's own closing period
      (protected separately below). Same root cause as Q24 above: Q45's
      own two solid-of-revolution diagrams (one per figure) and small
      axis-label formula images merge, by genuine Y-proximity, into
      regions whose combined raw extent also covers real statement prose
      physically positioned between/around them - `raw_intersects` is
      True throughout this group, so every fix here is at the exclusion-
      check layer, never the merge/growth/crop logic (Section 23).
    evidence: docs/phase-3o-report.md, Section Q
    status: reviewed
  - pdf_sha256: 5c8a08f7fdaccaa3b5661f40705e214bffdab634b845bce6288f8eeaa25db264
    page: 19
    bbox: [471.0, 181.0, 477.0, 193.0]
    rule: protect_from_region_membership
    question_id: enade-2008-computing-q45
    reason: >-
      "." (y0=182.3, x0=472.5) - the closing period of "como resultado da
      integral [inline formula image].", same line as the override above,
      split into its own fragment by the inline formula image between them.
    evidence: docs/phase-3o-report.md, Section Q
    status: reviewed
  - pdf_sha256: 5c8a08f7fdaccaa3b5661f40705e214bffdab634b845bce6288f8eeaa25db264
    page: 19
    bbox: [303.0, 322.0, 553.0, 335.0]
    rule: protect_from_region_membership
    question_id: enade-2008-computing-q45
    reason: >-
      "Com base nessas informacoes, julgue os itens a seguir." (y0=323.5,
      9.96pt) - the transition sentence introducing items I/II/III,
      sitting between the two solid-of-revolution figures. Same defect
      class as the intro clause above.
    evidence: docs/phase-3o-report.md, Section Q
    status: reviewed
  - pdf_sha256: 5c8a08f7fdaccaa3b5661f40705e214bffdab634b845bce6288f8eeaa25db264
    page: 19
    bbox: [317.0, 346.0, 561.0, 359.0]
    rule: protect_from_region_membership
    question_id: enade-2008-computing-q45
    reason: >-
      "Cada secao transversal do solido S obtida quando este" (y0=347.7)
      - item I's own first line, entirely missing before this override
      (item I's own marker alone survived, per docs/phase-3o-report.md
      Section Q's forensic scan).
    evidence: docs/phase-3o-report.md, Section Q
    status: reviewed
  - pdf_sha256: 5c8a08f7fdaccaa3b5661f40705e214bffdab634b845bce6288f8eeaa25db264
    page: 19
    bbox: [317.0, 358.0, 561.0, 371.0]
    rule: protect_from_region_membership
    question_id: enade-2008-computing-q45
    reason: >-
      "e interceptado em x = c por um plano paralelo ao plano" (y0=359.7) - item I's own second line, same defect class as above.
    evidence: docs/phase-3o-report.md, Section Q
    status: reviewed
  - pdf_sha256: 5c8a08f7fdaccaa3b5661f40705e214bffdab634b845bce6288f8eeaa25db264
    page: 19
    bbox: [317.0, 370.0, 561.0, 383.0]
    rule: protect_from_region_membership
    question_id: enade-2008-computing-q45
    reason: >-
      "yOz e um circulo centrado no ponto (c, 0, 0) e de raio" (y0=371.8) - item I's own third line, same defect class as above.
    evidence: docs/phase-3o-report.md, Section Q
    status: reviewed
  - pdf_sha256: 5c8a08f7fdaccaa3b5661f40705e214bffdab634b845bce6288f8eeaa25db264
    page: 19
    bbox: [317.0, 380.0, 533.0, 396.0]
    rule: protect_from_region_membership
    question_id: enade-2008-computing-q45
    reason: >-
      "medindo f(x) e, portanto, de area igual a (f(x))2." (y0=381.6) - item I's own fourth and last line, same defect class as above.
    evidence: docs/phase-3o-report.md, Section Q
    status: reviewed
  - pdf_sha256: 5c8a08f7fdaccaa3b5661f40705e214bffdab634b845bce6288f8eeaa25db264
    page: 19
    bbox: [317.0, 395.0, 561.0, 408.0]
    rule: protect_from_region_membership
    question_id: enade-2008-computing-q45
    reason: >-
      "Se P e uma particao uniforme do intervalo [a, b], sendo" (y0=396.2)
      - item II's own first line, entirely missing before this override
      (item II's own marker alone survived).
    evidence: docs/phase-3o-report.md, Section Q
    status: reviewed
  - pdf_sha256: 5c8a08f7fdaccaa3b5661f40705e214bffdab634b845bce6288f8eeaa25db264
    page: 19
    bbox: [317.0, 399.0, 430.0, 422.0]
    rule: protect_from_region_membership
    question_id: enade-2008-computing-q45
    reason: >-
      "P = {a = x0 < x1 < x2 < ..." (y0=400.0, 12pt - a larger math-mode
      font than the surrounding 9.96pt body prose) - item II's own second
      line, first fragment (the set-builder notation, split from its own
      continuation below by a font-size change PyMuPDF reports as a
      separate dict-mode line).
    evidence: docs/phase-3o-report.md, Section Q
    status: reviewed
  - pdf_sha256: 5c8a08f7fdaccaa3b5661f40705e214bffdab634b845bce6288f8eeaa25db264
    page: 19
    bbox: [427.0, 409.0, 561.0, 423.0]
    rule: protect_from_region_membership
    question_id: enade-2008-computing-q45
    reason: >-
      "< xn = b}, tal que )x = xi !x i-1," (y0=410.1) - continuation
      fragment of the same physical line as the override above (same_row
      ordering, already active for this booklet, reassembles the two
      fragments into one line).
    evidence: docs/phase-3o-report.md, Section Q
    status: reviewed
  - pdf_sha256: 5c8a08f7fdaccaa3b5661f40705e214bffdab634b845bce6288f8eeaa25db264
    page: 19
    bbox: [438.0, 431.0, 561.0, 445.0]
    rule: protect_from_region_membership
    question_id: enade-2008-computing-q45
    reason: >-
      "para ci 0 [ xi, xi ! 1], 1< i < n." (y0=432.1) - item II's own third line, same defect class as its first two lines above.
    evidence: docs/phase-3o-report.md, Section Q
    status: reviewed
  - pdf_sha256: 5c8a08f7fdaccaa3b5661f40705e214bffdab634b845bce6288f8eeaa25db264
    page: 19
    bbox: [317.0, 431.0, 348.0, 444.0]
    rule: protect_from_region_membership
    question_id: enade-2008-computing-q45
    reason: >-
      "entao" (y0=432.4) - item II's own fourth and last line (a single word
      introducing the conclusion carried by item II's own small formula
      image), same defect class as above.
    evidence: docs/phase-3o-report.md, Section Q
    status: reviewed
  # --- Phase 3Q: Q45's own item III residual (3 more lines, same oversized
  # raw-merge region as the 12 Phase 3O overrides above - not found during
  # Phase 3O's own investigation, which stopped once items I/II were
  # restored and mis-characterized the remaining gap as "one missing
  # character"). All three lines are genuinely present, unambiguous text
  # (confirmed via rawdict/texttrace - no custom font/glyph mapping
  # involved) - `state=contained` against the SAME region
  # (owner=objective-45, bbox=(318.6,168.5)-(559.2,493.3)) whose own grown
  # y1 (493.3) extends well past its own raw_bbox y1 (449.9) via label
  # absorption unrelated to these lines. The "e as retas x = 0 e x = 2."
  # fragment shares a physical row with "eixo x, o grafico de" (same
  # baseline) and same_row_ordering.py (Phase 3N) already correctly
  # orders the pair left-to-right - confirmed by direct instrumentation
  # that same-row reordering runs and succeeds *before* region exclusion
  # removes both; the same-row nuance documented in Phase 3O ("entao"
  # reading before "para ci...", item II's own residual, unrelated to
  # item III) is a separate, still-open mechanism, not touched here (see
  # docs/phase-3q-report.md, Section L for why the two symptoms are
  # independent).
  - pdf_sha256: 5c8a08f7fdaccaa3b5661f40705e214bffdab634b845bce6288f8eeaa25db264
    page: 19
    bbox: [317.0, 466.0, 561.0, 480.0]
    rule: protect_from_region_membership
    question_id: enade-2008-computing-q45
    reason: >-
      "torno do eixo x da regiao do plano delimitada pelo" (y0=467.9) -
      item III's own second line, continuing "III E igual a 2B o volume
      do solido gerado pela rotacao em" (already published).
    evidence: docs/phase-3q-report.md, Section K; data/manifests/source-token-ledger-2008.yaml
    status: reviewed
  - pdf_sha256: 5c8a08f7fdaccaa3b5661f40705e214bffdab634b845bce6288f8eeaa25db264
    page: 19
    bbox: [317.0, 482.0, 406.0, 495.0]
    rule: protect_from_region_membership
    question_id: enade-2008-computing-q45
    reason: >-
      "eixo x, o grafico de" (y0=483.3) - item III's own third line, first
      fragment (continues onto an inline formula image for the function
      "f", then the fourth override below).
    evidence: docs/phase-3q-report.md, Section K; data/manifests/source-token-ledger-2008.yaml
    status: reviewed
  - pdf_sha256: 5c8a08f7fdaccaa3b5661f40705e214bffdab634b845bce6288f8eeaa25db264
    page: 19
    bbox: [443.0, 482.0, 551.0, 495.0]
    rule: protect_from_region_membership
    question_id: enade-2008-computing-q45
    reason: >-
      "e as retas x = 0 e x = 2." (y0=483.3, x0=444.0) - same physical row
      as "eixo x, o grafico de" above (same_row_ordering.py already
      orders the pair correctly, left-to-right), continuation after an
      inline formula image for the function "f" between them.
    evidence: docs/phase-3q-report.md, Section K; data/manifests/source-token-ledger-2008.yaml
    status: reviewed
  # --- Phase 3S: Q45's own item III inline vector formula, preserved as a
  # dedicated visual asset instead of a text gap --------------------------
  - pdf_sha256: 5c8a08f7fdaccaa3b5661f40705e214bffdab634b845bce6288f8eeaa25db264
    page: 19
    bbox: [406.68, 481.92, 442.92, 493.44]
    rule: declare_inline_formula_region
    question_id: enade-2008-computing-q45
    reason: >-
      Item III's own printed sentence ("...delimitada pelo eixo x, o
      grafico de f(x) = sqrt(x) e as retas x = 0 e x = 2.") includes the
      function definition "f(x) = sqrt(x)" drawn entirely as vector
      artwork - page.get_drawings() reports exactly 9 path elements
      (fill+stroke glyph-shaped outlines for "f", "(", "x", ")", "=", the
      radical "sqrt" checkmark+overbar, and the second "x") whose own
      union bbox is (406.679,481.919)-(442.919,493.439), filling exactly
      the gap between "eixo x, o grafico de" (page 19, ends x1=404.97,
      y0=483.28) and "e as retas x = 0 e x = 2." (starts x0=443.97, same
      y0=483.28) - both already restored as text by the two
      protect_from_region_membership overrides directly above.
      page.get_text("rawdict")/get_texttrace() confirm zero spans/chars
      anywhere in this x-range on this row - this was never font text with
      a missing/ambiguous encoding (unlike D40's own "F"/"B" - Fase 3R),
      it is 100% vector-drawn artwork, never recoverable as canonical text
      without fabricating an unverified glyph sequence (PROMPT Fase 3Q
      Section 5/17, reaffirmed by Fase 3S Section 5). This declaration
      authorizes assembler.py's own _build_declared_inline_formula_regions
      to construct a dedicated VisualRegion for exactly this bbox (never
      trusted blindly - re-verified against the real PDF's own
      page.get_drawings() at extraction time, only ever activated when at
      least one genuine drawing is found there), rendered as its own
      tightly-cropped asset (type: equation, ~6pt fixed padding, never
      widened to this page's own column bounds) and inserted inline, at
      the correct point in item III's own reading order, between the two
      already-restored text lines above - never reusing or repositioning
      the question's own pre-existing, much larger figure-01.png (which
      happens to also show this formula, but is scoped to the intro's two
      solid-of-revolution diagrams, positioned far earlier in the
      statement, and includes far more surrounding text than this one
      formula alone - not a faithful, correctly-placed substitute).
    evidence: docs/phase-3s-report.md; data/manifests/source-token-ledger-2008.yaml
      (token q45-item-iii-function-formula); docs/phase-3q-report.md, Section K
    status: reviewed
  # --- Phase 3O: Q54's own opening clause, swallowed by the routing-table
  # figure's own grown region ------------------------------------------
  - pdf_sha256: 5c8a08f7fdaccaa3b5661f40705e214bffdab634b845bce6288f8eeaa25db264
    page: 23
    bbox: [63.0, 450.0, 320.0, 463.0]
    rule: protect_from_region_membership
    question_id: enade-2008-computing-q54
    reason: >-
      "No encaminhamento de pacotes na Internet, cabe a cada" (y0=451.7,
      9.96pt body prose) - the statement's own opening clause, currently
      cut short mid-sentence at "no". Absorbed as a label candidate purely
      by the font-size-gate rounding defect described above (same
      mechanism as Q02/Q07's own sentence/clause overrides), then excluded
      from canonical text by plain geometric containment in the routing-
      table diagram's own grown region (the table itself - "endereco
      de rede", "mascara", IP addresses, etc. - correctly remains image-
      only; this override touches only the one statement-prose line).
    evidence: docs/phase-3o-report.md, Section R
    status: reviewed
  # --- Phase 3O: D40's own two residuals (region-merge-content-loss) ----
  - pdf_sha256: 5c8a08f7fdaccaa3b5661f40705e214bffdab634b845bce6288f8eeaa25db264
    page: 17
    bbox: [119.0, 186.0, 285.0, 199.0]
    rule: protect_from_region_membership
    question_id: enade-2008-computing-d40
    reason: >-
      "data_nascimento, renda, idade)" (y0=187.5, monospace code font) -
      the Cliente(...) schema's own second printed line, completing
      "Cliente(nroCliente, nome, endereco," (already correctly rendered as
      a `code` content block). This code line is legitimately inside
      figure-02.png's own region (which exists specifically to show the
      primary key's own underline formatting, not representable as plain
      text) - `raw_intersects` is True (the region's raw bbox already
      spans this line from the start), but the region itself is seeded,
      and its own X-extent widened, by a false merge with an unrelated
      ruled "RASCUNHO" answer-grid on the same page's own right column
      (only ~129pt away, under this booklet's own 150pt
      region_merge_x_tolerance) - the merge itself is a separate, riskier
      fix (Section 23 last-resort); protecting this one code line keeps
      it as legible text without touching that merge, while figure-02.png
      itself (still showing the correctly-formatted schema image) is
      completely unaffected.
    evidence: docs/phase-3o-report.md, Section S
    status: reviewed
  - pdf_sha256: 5c8a08f7fdaccaa3b5661f40705e214bffdab634b845bce6288f8eeaa25db264
    page: 17
    bbox: [63.0, 214.0, 292.0, 227.0]
    rule: protect_from_region_membership
    question_id: enade-2008-computing-d40
    reason: >-
      "Para essa relacao, foram criados dois indices" (y0=216.0, 9.96pt
      body prose) - the opening clause of the paragraph that continues
      "secundarios: IndiceIdade, para o atributo idade...", currently
      starting mid-sentence at "secundarios:" without it. Same region,
      same false-merge root cause as the code line above.
    evidence: docs/phase-3o-report.md, Section S
    status: reviewed
  # --- Phase 3R: D40's own residual cosmetic "F" leak (query-tree operator
  # symbol, documented as an accepted trade-off since Phase 3G) ----
  - pdf_sha256: 5c8a08f7fdaccaa3b5661f40705e214bffdab634b845bce6288f8eeaa25db264
    page: 17
    bbox: [95.0, 574.0, 104.0, 588.0]
    rule: force_region_membership
    question_id: enade-2008-computing-d40
    reason: >-
      Standalone line "F" (bbox 95.759,574.538-103.201,587.914; charcode
      70, glyph_id 2, font PKHEMP+TT2F14o00, size 13.31pt) is not a Latin
      letter at all: font PKHEMP+TT2F14o00 is a small (367-byte), embedded
      CFF/Type1 subset used only twice on this page, for this glyph and for
      the sibling glyph at charcode 66 ("B", glyph_id 1) that appears
      immediately above it (bbox 123.239,536.978-131.515,550.354) - both
      sit inline with the query-tree diagram's own projection/selection
      row labels ("nome,endereco" under the first, "idade < 40 OR renda <
      30000" under the second), exactly matching the statement's own
      sentence "na qual B e F representam as operacoes de projecao e de
      selecao, respectivamente" (i.e. these two glyphs ARE the pi/sigma
      operator symbols the sentence describes). The font declares
      WinAnsiEncoding (PyMuPDF's fallback charcode->unicode decode, since
      the font carries no ToUnicode CMap), which is why the raw text layer
      reports literal "B"/"F" - but the font's own embedded glyph *program*
      paints something else entirely: rendering this exact page region at
      3x zoom (the same rendering enade's own asset pipeline already uses
      for figure-01.png) shows the real characters are the pi and sigma
      relational-algebra operators, not Latin letters (data/questions/2008/
      all-computing/enade-2008-computing-d40/figure-01.png, already
      published, already shows "pi_nome,endereco" / "sigma_idade < 40 OR
      renda < 30000" / "Cliente" in full - confirmed by direct visual
      inspection of that exact PNG). "F"'s own sibling glyph "B" is left
      untouched by this override (out of Fase 3R's own declared scope -
      see docs/phase-3r-report.md) and continues to publish exactly as
      before ("...respectivamente. B\tnome,endereco"), a separate,
      pre-existing, undocumented-as-blocked artifact this phase does not
      touch. "F" alone reaches `_text_consumption_decision` as "ambiguous"
      (state="touching": h_overlap<=0 since the region's own grown bbox
      left edge, 103.32, sits 0.12pt right of "F"'s own x1=103.2 - neither
      `raw_intersects` (28.8pt short of the region's raw bbox left edge,
      132.0) nor `matches_absorbed_label` (excluded from label-absorption
      candidacy entirely: its own 13.31pt font size is *larger* than this
      page's own body font size, failing figures.py's own
      FONT_SIZE_CAPTION_MARGIN candidate filter - the same filter that
      correctly protects genuine large-font body prose from being wrongly
      swallowed into a figure crop elsewhere in this corpus) - so growth
      never even considered it, and the tri-state consumption policy
      correctly refuses to guess from geometry alone (PROMPT Fase 3G,
      "ambiguous never authorizes destructive removal"). This override
      supplies the individually-reviewed evidence that policy withholds by
      design: figure-01.png already shows this exact glyph's true visual
      identity in full (the render clip in assets.py's own `_render_bbox`
      extends left to the page's own content margin, well past this line's
      x0, independently of the narrower logical region bbox
      `_line_in_region` uses for text-exclusion) - removing this one
      duplicate text line loses no information the reader cannot already
      see, and does not touch growth, merge, or crop logic at all.
    evidence: docs/phase-3r-report.md; data/manifests/source-token-ledger-2008.yaml
      (token d40-tree-selection-operator-sigma);
      data/questions/2008/all-computing/enade-2008-computing-d40/figure-01.png
    status: reviewed
